# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Frontend deploy on main branch commit
'on':
  push:
    branches:
      - main
jobs:
  run_tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v3
        with:
          node-version: 18
          cache: 'npm'

      - run: npm ci

      - run: npx nx run-many --target=test --projects=tag:type:ui

  build_and_deploy:
    needs: run_tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v3
        with:
          node-version: 18
          cache: 'npm'

      - run: npm ci

      - run: npx nx run-many --target=build --projects=tag:type:ui

      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_TWO_WAY_C1E54 }}'
          channelId: live
          projectId: two-way-c1e54
